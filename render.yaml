services:
  - type: web
    name: taskybot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
